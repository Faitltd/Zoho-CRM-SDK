# API Baselines

This folder stores the API surface baseline used by automated semver checks.

- `zoho-crm.api.json` is generated by `npm run api:baseline`.
- `baseline-version.json` records the package version the baseline corresponds to.

Update the baseline only when intentionally releasing a new public API surface.
